# Zsh Configuration

Personal Zsh shell configuration with custom aliases, functions, and environment settings.

## Setup

Source files in order:

- `_*.zsh` - First (environment setup)
- `*.zsh` - Main configuration
- `z_*.zsh` - Last (final overrides)
